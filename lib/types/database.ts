// Generated via `supabase gen types typescript --project-id <id> > lib/types/database.ts`.
// Placeholder until the Supabase project exists and codegen is run for real — `any` here
// (rather than a stricter placeholder) avoids the Supabase client's generic constraints
// collapsing query results to `never` before real types exist.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
